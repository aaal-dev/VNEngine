#pragma once

#include "../config/configmanager.hpp"
#include "../log/log.hpp"

class VideoManager
{
	// Variables
	static Log*           log;
//	static ConfigManager* configManager;
//	Config*               config;
	
public:
	VideoManager();
	virtual ~VideoManager();

};

